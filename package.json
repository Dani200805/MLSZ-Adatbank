{
  "name": "mlsz-adatbazis",
  "version": "1.0.0",
  "description": "MLSZ Adatbázis – Magyar labdarúgó csapatok és játékosok nyilvántartása",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "server": "json-server --watch server/db.json --port 3001"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
